<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Página em Fogo</title>
  <style>
    body {
      margin: 0;
      background-color: black;
      font-family: 'Courier New', monospace;
      overflow: hidden;
      color: white;
      position: relative;
    }

    #text-container {
      position: absolute;
      top: 10%;
      left: 5%;
      right: 5%;
      font-size: 22px;
      line-height: 1.6;
      z-index: 2;
      white-space: pre-wrap;
      animation: fadeOutText 6s ease-in-out forwards;
      animation-delay: 4s;
    }

    #background-image {
      position: absolute;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      opacity: 0.1;
      background-image: url('https://images.unsplash.com/photo-1534447677768-be436bb09401?auto=format&fit=crop&w=1350&q=80');
      background-size: cover;
      background-position: center;
      z-index: 1;
      transition: opacity 3s ease-in-out;
    }

    #fire-overlay {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: url('https://i.imgur.com/o7fG8xg.gif');
      background-size: cover;
      opacity: 0;
      z-index: 3;
      pointer-events: none;
      animation: fadeInFire 3s ease-in-out forwards;
      animation-delay: 3s;
    }

    @keyframes fadeOutText {
      to { opacity: 0; transform: scale(0.95); filter: blur(4px); }
    }

    @keyframes fadeInFire {
      to { opacity: 1; }
    }

    #fragment {
      position: absolute;
      bottom: 40px;
      right: 40px;
      width: 80px;
      height: 80px;
      background-image: url('https://i.imgur.com/zYIlgBl.png');
      background-size: contain;
      background-repeat: no-repeat;
      z-index: 4;
      opacity: 0;
      cursor: pointer;
      animation: revealFragment 2s ease-in-out forwards;
      animation-delay: 10s;
    }

    @keyframes revealFragment {
      to { opacity: 1; transform: scale(1.05); }
    }

    #ash-overlay {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: url('https://i.imgur.com/jK0O6eJ.png') repeat;
      opacity: 0;
      z-index: 2;
      animation: ashFall 6s linear forwards;
      animation-delay: 3s;
      pointer-events: none;
    }

    @keyframes ashFall {
      to { opacity: 0.4; transform: translateY(100px); }
    }
  </style>
</head>
<body>

<audio autoplay>
  <source src="https://freesound.org/data/previews/527/527079_8044-lq.mp3" type="audio/mpeg">
</audio>

<div id="background-image"></div>
<div id="text-container"></div>
<div id="fire-overlay"></div>
<div id="ash-overlay"></div>
<div id="fragment" title="Clique para continuar"></div>

<script>
  const text = `“E então, quando tudo parecia calmo... a página começou a arder.”\n\nNada era por acaso.\nAs palavras carregavam sinais.\nAs cinzas sabiam mais do que os olhos.`;
  const container = document.getElementById("text-container");
  let i = 0;

  function typeText() {
    if (i < text.length) {
      container.textContent += text.charAt(i);
      i++;
      setTimeout(typeText, 80);
    }
  }

  // Início da digitação
  setTimeout(typeText, 1000);

  // Redirecionamento após final cinematográfico
  document.getElementById("fragment").onclick = () => {
    window.location.href = "pagina-eco.html";
  };

  setTimeout(() => {
    window.location.href = "pagina-eco.html";
  }, 15000);
</script>

</body>
</html>
